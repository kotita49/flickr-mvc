class PhotoStream {
    constructor(imgurl, authorurl, name, title, tags){
            this.imgurl = imgurl,
this.authorurl = authorurl,
this.name = name,
this.title = title,
this.tags = tags
                 }
       
       
}
module.exports = PhotoStream;